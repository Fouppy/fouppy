{
  "$schema": "https://turbo.build/schema.json",
  "pipeline": {
    "build": {
      "outputs": [
        ".next/**",
        "!.next/cache/**",
        "!public/*.xml",
        "!public/robots.txt"
      ]
    },
    "lint": {}
  }
}
